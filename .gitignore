# .NET build trees
wasm/bin/
wasm/obj/
test/engine/bin/
test/engine/obj/

# The published runtime bundle (web/_framework) IS committed so the branch is testable
# without the toolchain — but the brotli/gzip duplicates the dev server doesn't use are not.
web/_framework/*.br
web/_framework/*.gz

*.wav
node_modules/
.DS_Store

# Release artifact (built by `make release`, uploaded to GitHub releases)
skafinity-web.tar.gz

# `make dist` output: the GH-Pages site tree + the ~10 MB single-file build. Both are
# derived from web/ on demand — the target is committed, never the artifacts.
dist/
