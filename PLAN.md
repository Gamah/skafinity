# PLAN

**Everything here must keep working in s&box.** The engine is shared source, not a port:
`sbox-library/Skafinity/Code/Engine/**` compiles into both the s&box library and the web wasm bundle.
So every row below is bound by the same rules — the engine stays framework-free (`System`,
`System.Collections.Generic`, `System.Text` only; no `Sandbox.*`, no Emscripten-isms), anything
web-specific goes in `wasm/Exports.cs`, and any new `Config` field lands in `Cfg.To`/`Cfg.From`
(+ `Cfg.Size`) or it won't survive the JS boundary. `SkafinityPlayer.cs` and `Code/UI/` are
s&box-only and must still compile and drive the engine after each row.

Seed compatibility is **not** preserved: old `vibe:tag:n` seeds are expected to break, and the
audio a seed renders is expected to change from commit to commit. That is accepted — no
back-compat shim, no golden-audio contract, and a changed render is never a regression. The
only reproducibility that matters is *within* a build (web and s&box agreeing, and a pure
refactor proving it moved nothing).

| Rank | Item | Notes |
|---|---|---|
| 45 | The per-genre tempo bands and saturation points are guesses; break them down against real music | `GenreProfile` now carries `BpmMin`/`BpmMax`, `FastBpmMin`/`FastBpmMax` and `TempoFloor`/`TempoCeil` per genre — 36 numbers, and **every one of them was chosen by argument rather than by looking at records.** The mechanism is settled and the suite asserts it (a genre's bands sit inside its saturation, the knob's ends never leave it, the clamp is not vacuous); what is unproven is the values. This is a *study* row, not a code row: take each of the six genres, gather actual tempos across a spread of well-known tracks, and see where the bands and the saturation points should really sit — including whether a genre wants a wider or narrower spread between its ordinary and uptempo bands, and whether some genres want no uptempo band at all. Sweep with `-- --seed vibe:tag:n`, which prints the drawn tempo, the genre's range and whether the knob saturated. Same exercise is worth doing for the grooves and the accent weights while the records are open. Re-bless the digests; `--levels` unaffected. |
| 35 | Metal's "double kick" groove is an unbroken sixteenth kick for the entire song, at any tempo | `DrumGroove.Metal[0]` is `S(0,0,0,0, …)` — a kick on every sixteenth, one bar, looped for three minutes. At the top of the genre's band that is ~13 hits a second with no variation anywhere, which is what reads as a blast beat, and it reads that way regardless of subdivision or tempo: the tell is that nothing ever changes. A real drummer plays double-kick in BURSTS under a riff — under the chorus, into a fill, for two bars of a verse — and rides an ordinary backbeat the rest of the time. The mechanism already exists: `Pattern` carries its own length, so a two- or four-bar kick pattern whose later bars drop to a backbeat is the same one object. Worth checking the other genres for the same shape while in there (punk's cymbal "never stops" is deliberate; metal's kick was not a decision). Re-measure `--levels` — thinning the kick moves the genre's drum reference, which every other voice's dB is measured against. |
| 28 | There is no actual BEND in the engine — only a lean into pitch, and its depth is not a parameter | `Expression.cs` rolls four gestures and the two pitch ones are hardcoded: `BendIn` sets `BendSemis` to −0.3 or −0.55 semitones over 90 ms, `Scoop` sets `ScoopSemis` to 0.15 or 0.3. Those are *approach* gestures — a singer or a horn easing onto the note — and they are right for what they are. What is missing is the guitar bend a listener would name as one: a **whole step or a semitone, bent up and HELD**, or bent up to the chord tone above and released, which is a different musical event and lands in the middle of the note rather than at its front. The timing side is already correct (`BendTime` is in SECONDS, so a bend does not scale with tempo — keep it that way). The gap is that depth is fixed while only the *propensity* is configurable (`LeadGtrBend` → `Expr()`), so a genre cannot say "bends deep" vs "bends barely", and there is no target-pitch bend at all. Wants a `BendSemis` range on the propensity struct (or a separate depth field) plus a held-bend variant in `Voicing`, and the synth already reads `BendSemis`/`BendTime`, so the render side may need nothing. Interacts with the country row below — that row is about *how often* country bends, and it is hard to judge frequency while every bend is the same shallow one. Re-bless; `--levels` unaffected. |
| 25 | Voice leading never reassigns chord tones between voices, so a stepwise root move still slides the whole shape | `Harmony.PlanVoiceLeading` octave-shifts each voice to the inversion nearest the chord before it, which killed the octave-sized parallel leap — but voice *i* is always the same degree offset of the voicing, so a root move of a fourth (I→IV, under a tritone already) moves every voice by that fourth. No common tone, just a smaller parallel slide, which is the other half of "the comp sounds like one part rather than a chord calculator". The fix is to let the chord ROTATE: voice *i* plays voicing offset `(i + r) mod n`, with the rotation chosen per chord by the same cycle-closing walk that picks the octaves (they compose — a rotation is chosen alongside an octave, not instead of it). The obstacle is that the pitch array's INDEX currently means "voicing slot": `DrivenVoicing` in `Guitar.cs` drops the third by testing `_voicing[i]` against the *i*th pitch, so a rotated chord must carry its offsets alongside its pitches or the driven guitar starts dropping the wrong note. Watch the strum too — `RenderStrumBar` nudges `tones[i]` by `i * 4 ms`, so array order is the order the pick crosses the strings. Re-measure `--levels`: moving which voice is on top moves the comp's average register. |
| 25 | The ending chord is the one place a comp still leaps | `EndingChord` builds from `VoicedMidis` (root position) rather than through `ChordMidis`, deliberately: a song should land where the genre voices its final chord, not where the last change happened to leave the register. That is a judgement made without listening to it — the last chord of every song can still jump a seventh from the chord before it, which is exactly the "it jumped" the voice-leading row was about, sitting on the most exposed moment in the song. Listen to a few endings per genre (`--score <seed>` over the last bars against `--seed`'s `voicelead` line) and decide: keep it anchored, voice-lead it like any other change, or voice-lead it and let the ritard cover the move. Cheap either way — it is one call site. |
| 25 | Country's lead bends into half its notes and scoops half of them | `Expr("LEAD GTR")` gives genre 2 `BendIn` and `Scoop` of at least 0.5 each (`MathF.Max( _c.LeadGtrBend, 0.5f )`, both propensities off the same value), against 0.30 vibrato. That was set when country's lead was dyads, where the parallel bending was the thing that read as a horn; the pair is gone, so the propensity is now exposed on a single line and nothing has judged it on its own. If country's lead reads seasick rather than twangy, this is the knob and not the notes — and note `BendIn` and `Scoop` do not have to move together, a bend UP INTO the note is the telecaster gesture and a scoop within it is a different one. Purely a listening call: change the numbers, `--levels` is unaffected, re-bless. |
| 25 | 211 chord changes still move a voice more than a tritone | Down from 765 in root position, and every survivor is optimal for the CYCLE — one wider move that buys smaller ones on the other three changes, which is the solution rather than a failure of it (the engine test asserts the count, not that it is zero). Unlistened-to: the maths says these are the right trade and only an ear says whether a single voice jumping a minor sixth while the others hold reads as a re-voicing or as a fault. `--score` over a seed whose `voicelead` line is non-zero is the way in. If they do read badly the lever is the cost function in `PlanVoiceLeading` — weighting the WORST edge rather than the total would spread the motion, at the price of moving more voices on average. |
| 22 | Preview a branch on the live Pages site without merging — blocked on one repo setting | Wanted workflow: run the Pages workflow by hand against a feature branch, let the live site serve that branch until master is next pushed. `pages.yml` already has `workflow_dispatch`, so the Actions UI already offers a ref picker and the `build` job would run fine on a branch — it checks out the ref, runs `make check-bundle`, `make dist` and `test/dist-single.mjs` exactly as on master. **The `deploy` job is what stops it.** It declares `environment: github-pages`, and that environment currently carries a deployment branch policy (`protected_branches: false`, `custom_branch_policies: true`) whose only entry is `master` — so a dispatch from any other branch builds, then fails at deploy with a "not allowed to deploy to github-pages" error. Two ways to unblock, both a **repo settings change, which is Gamah's call, not a code change**: add a wildcard branch policy (`*`, or something like `preview/*` so it is deliberate), or drop the branch policy entirely. Prefer a named prefix — an unrestricted policy means any branch push that ever gains a Pages trigger can replace the live site. Once it is open, note the two consequences and write them into `CLAUDE.md`: there is ONE Pages site per repo, so a branch preview REPLACES the live site until master redeploys (`concurrency: group: pages` queues rather than cancels, so the next master push restores it), and the live site can therefore be a branch — a future session must not read "the site does not match master" as a stale-bundle bug. Worth remembering what a real deploy adds over serving `dist/` locally, which is less than it looks: only Jekyll's underscore-directory behaviour (already handled by the `.nojekyll` `make dist` writes) and the `/skafinity/` subpath (already handled by every path in `web/` being relative). The browser-only gaps — the blob-URL `Worker`, `AudioContext`, the DOM wiring in `app.js` — are untested either way. |
| 20 | A genre's comp sits 4 dB louder on some seeds than others, and nothing is watching the spread | Measured while shipping the 32nd row: rock's rhythm guitar averages −4.6 dB under its kit (`--levels`, which measures the genre) while the single seed the engine suite balances on sits at −0.7 dB. Same config, same balances — the difference is which comp figure each SECTION happened to draw, since `RenderSection` re-draws per section from the genre's table and the figures are not equally dense. That is a 4 dB swing in how loud the backing is, decided by a seed, and it is invisible to both tools: `--levels` averages it away and the suite's `comp sits under the kit` check is one seed (which is why its threshold had to be the loose "not louder than the kit" — see the comment there). Two things to work out: whether the spread is audible enough to care about (render the same tag across a few n and listen), and if so whether the fix is to level figures against each other (a density-weighted gain on the comp voice) or to stop the sparse and dense figures from differing so much in the first place. Not a bug until someone has listened; it is a measurement with nobody's name on it. |
| 14 | Non-4/4 | `Timing` already carries `BeatsPerBar` and a `BeatGrouping` array (nothing reads the grouping yet), `ComposePlan` picks the meter in one place, and patterns are meter-agnostic now — a `Pattern` carries its own tick length and nothing in a voice assumes eight cells to a bar. 6/8 and 12/8 first (12/8 is the natural home of a one-drop shuffle; a country bass lesson in 6/8 turned up in the research). Compound meter wants a `BeatTicks` on `Timing` (48 simple, 72 compound) rather than a second grid. Changing meter *within* a song is the section map's job — `Part` already carries per-bar beat counts for anomalous measures, so it is the same mechanism widened. |
